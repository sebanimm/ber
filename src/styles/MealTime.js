import MealTimeMain from "./MealTimeMain";
import styled from "styled-components";

const MealTime = styled(MealTimeMain)`
	margin-bottom: 7%;
	font-size: 20px;
	height: 16%;
`;

export default MealTime;
